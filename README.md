# CustomCNN_vs_ResNetFinetuning
Implementing CNN building blocks (convolution, pooling, filters) from scratch in NumPy, then training a custom CNN and fine-tuning a pretrained ResNet-18 on the Oxford-IIIT Pet Dataset using PyTorch and Weights &amp; Biases.
